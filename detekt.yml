YawnDoctor:
  active: true
  QueryInsideLoop:
    active: true
    queryConstructors:
      - query
      - createYawnCriteria
      - createCriteria
    queryTerminals:
      - list
      - first
      - single
      - count
    iterationFunctions:
      - forEach
      - map
      - flatMap
      - associate
      - onEach

  MaterializedCount:
    active: true
    queryConstructors:
      - query
      - createYawnCriteria
      - createCriteria
